# 更新记录

## 1.1.12

- 修复存量历史记录因缺少 NOTE / DJ RATE 参考而无法打开已关联曲目的问题；导航复用曲目索引，成绩挂载仍需通过原有校验。
- 启动时从本地相同且已验证的成绩离线补齐旧历史参考，不改写历史成绩、达成时间或不同记录的参考依据。
- 从历史成绩进入曲目详情时，谱面详情默认选中该成绩的模式和难度；重复进入不会被上次详情快照覆盖，普通曲库入口保留原有默认难度和首页快照。

## 1.1.11

- 修复首页、曲目详情和谱面浏览中长版本名称挤压曲名的问题：版本仅与曲风共享一行，曲名区域独立排版。
- 首页长曲名沿用详情页的自动横向滚动，三处复用头部布局与共享元素动画。
- 折叠筛选摘要的“仅筛选”改为“仅搜索”，版本采用代数缩写，家用版和 substream 分别使用 CS、sub。

## 1.1.10

- 筛选区统一使用 32dp 高的紧凑控件，搜索范围与曲目类型改为带行标题的多选按钮。
- 等级、版本支持展开网格多选：等级每行 4 项、版本每行 2 项；版本按 CS、substream、历代顺序排列，11 代起加编号。
- 多选状态支持重置及页面返回保留，长版本列表可滚动，展开动画遵循“禁用视觉效果”设置。

## 1.1.9

- 曲目列表、曲目信息和谱面浏览的版本标记区分删除曲与家用版：删除曲保留红字，家用版改为蓝字“家用版”。
- 新增独立的“不显示家用版”筛选；“不显示删除曲”仅隐藏删除曲。两项同时开启时，折叠摘要显示“不显示删除曲/家用版”，重置与清除筛选同时恢复两项。

## 1.1.8

- 曲名优先使用保留符号的规范化精确匹配，修复 `∀`、`≡＋≡` 等纯符号标题无法关联的问题；原字母/数字规则作为兜底。
- 添加已确认的 `CODE:Ø`、`FiZZλ_PØT!OИ`、`POLꓘAMAИIA`、`uәn`、`Χ-DEN` 五项曲目别名。
- 升级后离线重建 BJM 关联索引，成绩 NOTE / EX / DJ RATE 校验保持原有规则。

## 1.1.7

- 完整解码 Textage 与 BJM 曲名中的 HTML 实体，修复 `Amor De Ver&atilde;o` 等曲目无法对应的问题，并正确保留补充平面 Unicode 字符。
- 升级时离线迁移旧曲库文字并重建匹配索引，保留谱面 ID、成绩及登录状态。
- 增加基于公开曲库快照的离线匹配核对工具和完整未匹配清单；相近曲名不自动关联。

## 1.1.6

- 雷达网格外圈改为 100，超过 100 的顶点按比例延伸，200 对应两倍半径。
- 固定预留完整的 200 范围及顶点间距，避免触碰上方难度选择和右侧数值；维度文字允许位于雷达延伸范围内，保留难度切换的平滑过渡。

## 1.1.5

- 固定雷达标签与数值列宽度，避免不同数值位数和最高维度加粗导致雷达及数值位置偏移。
- 切换有雷达数据的难度时，各顶点与填充色平滑过渡；快速连续切换从当前形状继续，启用“禁用视觉效果”时直接切换。
- 动画仅更新雷达多边形，网格和文字布局继续复用缓存。

## 1.1.4

新增：

- 曲目筛选增加“不显示删除曲”，与版本、等级筛选叠加，并支持重置。

优化：

- 详情和播放器从屏幕底边外垂直进入，前景内容与卡片背景缩放分离，避免从中央或右下方进入。
- 返回曲库时，上下方曲目与主卡片收缩同步移入，控制区同步显现，避免等待收缩结束后再出现。
- 详情与播放器从下方进入时使用图层平移；雷达网格、路径和文字布局按尺寸与数据缓存，减少动画中的重复计算。
- 其他难度的预解析延后至切换之后，先检查磁盘缓存，最多串行准备两个难度；切换和返回时取消，低内存设备跳过。
- 启动加载页提供“禁用视觉效果”入口，不增加隐藏页面或整批动画资源预加载。
- 增加沿用现有签名的 R8 Release 构建，用于实机性能测试；保留 Rhino 解释器所需的反射类。

## 1.1.3

调整：

- 删除曲曲名恢复普通颜色，仅保留版本右侧的红字标记；移除曲目及谱面详情中的 BJM 数据说明横幅。
- 雷达区域改名“谱面详情”，移至谱面入口卡片下方，难度选择按钮与标题同行。
- 无雷达时统一显示“暂无雷达数据”，移除操作提示及数据版本说明。
- 缩小雷达维度数值行间距，数值统一左对齐，最高维度的名称与数值均加粗。
- 谱面详情与播放器统一从下方进入，禁用视觉效果时直接显示。
- 切换谱面时待目标数据就绪再替换当前画面，快速加载不再闪现 Textage 提示；连续切换或返回时取消旧请求。

## 1.1.2

调整：

- 未收录版本统一在版本右侧显示红字“删除曲”，曲库、曲目详情和谱面浏览保持一致。
- 移除标题下方及说明区域中重复的分类标记，保留 BJM 数据差异说明。

## 1.1.1

调整：

- 保留 Textage 当前街机收录标记，已删除及家用版曲目使用红色标题并显示未收录说明。
- 优先选择当前收录的曲目版本，成绩历史在通过 NOTE 数及成绩校验后优先匹配当前收录谱面。
- 兼容旧曲库缓存；升级后自动同步补充收录状态，旧版成绩和谱面仍可保留浏览。

## 1.1.0

新功能：

- 曲目详情新增六维谱面雷达和独立难度切换栏，使用 BJM 雷达与 NOTE 数据并支持本地缓存。
- 雷达按最高维度着色；NOTE 数与当前谱面不一致时不展示该雷达。

调整：

- 谱面浏览成绩下方显示达成时间，并与曲目详情的成绩块一起过渡。
- 首页筛选增加展开与收起动画，遵循“禁用视觉效果”设置；搜索维度改为三个相连的多选按钮。

问题修复：

- 成绩依附前校验 BJM 参考 NOTE 数、EX SCORE 范围和 DJ RATE，避免将不同 NOTE 数的版本成绩附到同名谱面。
- 旧成绩保留在历史中；同步同一条成绩可补充校验依据，不重复生成历史记录。

## 1.0.11

调整：

- 增加可通过 ADB 开启的 BJM 登录诊断，记录 Cookie 存在状态、认证结果、刷新流程与持久化检查点，便于排查跨天登录问题。
- 诊断日志不记录账号、Cookie 值、令牌、响应正文或完整 URL。

## 1.0.10

问题修复：

- 恢复 0.7.11 的播放器局部刷新和谱面索引，避免每帧扫描整张谱面及反复计算 BPM 时间。
- 恢复可见音符筛选和绘制对象复用，保留跨越可视区域的长音符。
- 修复缺少元数据 NOTE 总数时，长音符总数随播放进度变化的问题。

## 1.0.9

问题修复：

- 恢复 BJM 会话持久化和一次静默恢复重试，启动或同步失败不再自动清除凭证及本地用户。
- 登录页优先恢复已有会话，WebView 与原生请求同步 Cookie。
- 恢复已发布版本中的 DP FLIP 与原有设置存储兼容性。

## 1.0.8

问题修复：

- 播放器切换 SP/DP 后同步曲目详情的模式，返回后再次进入使用当前模式。
- 模式按钮显示与实际谱面保持一致；无对应谱面时不切换到错误的模式。

## 1.0.7

调整：

- 曲目详情的成绩时间移到右侧成绩下方，右对齐显示。
- SP/DP 按钮可见高度缩小为 36dp，与用户头像一致，并保留触控区域。

## 1.0.6

问题修复：

- 去除曲目详情 NOTES 行额外的左边距，与难度和成绩日期左对齐。

## 1.0.5

调整：

- 恢复曲目详情难度卡片的原背景、圆角和边框样式。
- 成绩日期显示在难度与 NOTES 下方，保持卡片原有高度。

问题修复：

- 列表与详情使用同一套共享过渡，实现对称的展开与收起。
- 返回动画结束前隐藏列表原位内容，避免与移动元素重叠。

## 1.0.4

调整：

- 难度按钮与详情卡片共用连续的难度色块、圆角和边框，展开时保留颜色。
- 减少列表过渡中的共享元素计算，取消整张列表的透明度动画。
- 快捷难度改用轻量横向布局，减少列表中的嵌套懒加载开销。

## 1.0.3

调整：

- 列表快捷难度与曲目详情卡片、播放器难度按钮共享过渡位置。
- 快速打开谱面时，曲目信息、版本、BPM、难度和 SP/DP 按钮连续移动。

问题修复：

- 固定 SP/DP 按钮尺寸，避免页面过渡中拉高。
- 恢复曲库和历史页后台保留，返回时复用页面、筛选索引与滚动位置。
- 设置覆盖原页面，返回后恢复进入前的页面状态。

## 1.0.2

调整：

- 版本、BPM 和 SP/DP 按钮参与曲目页共享过渡。
- 难度列表收拢为顶部选择按钮，所选成绩上移，播放器从底部进入，NOTES 淡入。
- 过渡速度提高为 1.5 倍，主要位移动画从 360ms 缩短为 240ms。

问题修复：

- 消除曲目详情与播放器切换时固定信息的淡出闪烁。

## 1.0.1

调整：

- 曲目卡片展开为曲目详情，难度卡片展开为谱面播放器；返回时反向收起。
- 新增“禁用视觉效果”开关，默认关闭，开启后直接切换页面并保存偏好。
- 页面切换保留搜索、筛选和滚动位置。

问题修复：

- 设置返回进入前的页面，保留菜单单选与矩形间隔样式。

## 1.0.0

- 删除已弃用的 Vite / JavaScript 原型，统一为原生 Android 工程。
- 按 app、data、domain、ui 分层；拆分主 Activity、播放器、解析器和数据模型。
- 使用 ViewModel 管理状态和异步任务，集中处理系统导航事件。
- 合并重复索引更新逻辑，移除未使用的方法，保留本地数据兼容性。
- 统一主题、标题栏、卡片、边框和文字层级；补充曲库空状态与曲目数量。
- 改善 HYPER 难度文字对比度和触控区域，统一登录页样式。
- 适配登录页的系统返回手势，避免退出后仍处理登录回调。
- 版本号更新为 1.0.0，Android versionCode 递增为 2。
