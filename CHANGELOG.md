# 更新记录

## 1.0.2

调整：

- 版本、BPM 和 SP/DP 按钮参与曲目页共享过渡。
- 难度列表收拢为顶部选择按钮，所选成绩上移，播放器从底部进入，NOTES 淡入。
- 过渡速度提高为 1.5 倍，主要位移动画从 360ms 缩短为 240ms。

问题修复：

- 消除曲目详情与播放器切换时固定信息的淡出闪烁。

## 1.0.1

调整：

- 曲目卡片展开为曲目详情，难度卡片展开为谱面播放器；返回时反向收起。
- 新增“禁用视觉效果”开关，默认关闭，开启后直接切换页面并保存偏好。
- 页面切换保留搜索、筛选和滚动位置。

问题修复：

- 设置返回进入前的页面，保留菜单单选与矩形间隔样式。

## 1.0.0

- 删除已弃用的 Vite / JavaScript 原型，统一为原生 Android 工程。
- 按 app、data、domain、ui 分层；拆分主 Activity、播放器、解析器和数据模型。
- 使用 ViewModel 管理状态和异步任务，集中处理系统导航事件。
- 合并重复索引更新逻辑，移除未使用的方法，保留本地数据兼容性。
- 统一主题、标题栏、卡片、边框和文字层级；补充曲库空状态与曲目数量。
- 改善 HYPER 难度文字对比度和触控区域，统一登录页样式。
- 适配登录页的系统返回手势，避免退出后仍处理登录回调。
- 版本号更新为 1.0.0，Android versionCode 递增为 2。
